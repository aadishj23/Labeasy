DATABASE_URL="postgresql://user:password@localhost:5432/labeasy"
JWT_SECRET="jwt_secret"
RESEND_API_KEY=
RESEND_FROM_EMAIL=
# Public base URL of the site (used for sitemap.xml / robots.txt / canonical URLs).
NEXT_PUBLIC_SITE_URL="https://labeasy.aadishjain.dev"
# Admin account email (reserved — cannot be used for patient/lab signup).
# The admin password lives (bcrypt-hashed) in the DB "Admin" table; reset it via
# the admin page's OTP forgot-password flow (code sent to this address).
ADMIN_EMAIL=
